一、栈的定义
  受限制的线性表，只能从一段插入或删除，基本操作push和pop

二、栈的实现方式
1、链表实现（链式栈）
优点：不需要预先设定栈的容量，天然的动态扩容，
缺点：每操作一次push或pop都会调用一次malloc或free，很耗时。
2、数组实现（顺序栈）
优点：每操作一次push或pop不需要调用一次malloc或free，省时。速度快
缺点：需要预先设定栈容量，当栈容量不足时动态扩容比较耗时（需要拷贝）

三、栈的应用
1、函数调用
2、表达式求值（可使用两个栈）
3、括号匹配